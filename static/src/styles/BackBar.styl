.BackBar {
  background-color: white;
  padding: grid(1.5);
  box-shadow: 0px 2px 6px rgba(0,0,0,0.05);
  display: flex;
  align-items: center;
}

.BackBar__backButton {
  flex: 0;
}

.BackBar__branding {
  flex: 2;
}